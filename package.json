{
  "name": "your-app-name",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://nikanzh519.github.io/your-repo-name",
  "dependencies": {
    // your dependencies here
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
